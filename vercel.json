{
  "version": 2,
  "installCommand": "python -m pip install -r api/requirements.txt && cd frontend && npm ci",
  "buildCommand": "cd frontend && VITE_DATA_MODE=snapshot npm run build && mkdir -p ../api/static && cp -R dist/. ../api/static/",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/index.py": {
      "includeFiles": "{api/data/preview.db,api/static/**,backend/app/**}"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.py" }
  ]
}
